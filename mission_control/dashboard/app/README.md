# Harvy RL Bot — Mission Control Dashboard

React + Vite + Tailwind CSS + shadcn/ui dashboard for monitoring and controlling the Harvy RL Bot.

The dashboard talks to the FastAPI bridge running on port `9877`.

## Scripts

- `npm run dev` — start the Vite dev server
- `npm run lint` — run ESLint
- `npm run build` — production build
